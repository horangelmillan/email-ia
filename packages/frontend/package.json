{
  "name": "@email-ia/frontend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "ui5 build --all --clean-dest --dest dist",
    "start": "ui5 serve --port 8080 --open index.html",
    "typecheck": "echo \"No TS typecheck for UI5 sources (JS/XML)\"",
    "lint": "eslint webapp --ext .js",
    "test": "echo \"UI tests via Playwright (Fase 2+)\""
  },
  "devDependencies": {
    "@ui5/cli": "^4.0.14"
  }
}
