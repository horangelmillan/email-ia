# @email-ia/electron

Contenedor Electron (Main + Preload). Scaffolding pendiente de Fase 2:

- vite-plugin-electron (desarrollo, HMR) + electron-builder (distribución)
- Electron únicamente como contenedor: el Core no debe depender de Electron
- Spike pendiente: validar reutilización Electron ↔ Web
